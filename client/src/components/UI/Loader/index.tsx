// Components
import { Icon } from '../../index';


const Loader = () => {
  return (
    <Icon name="loader-spinner" width={48} height={48} className="text-primary-400" />
  );
};

export default Loader;